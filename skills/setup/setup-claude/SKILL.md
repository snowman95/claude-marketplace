---
name: setup-claude
description: Claude Code 개발 환경 전체를 구성합니다 — statusline과 LSP를 순서대로 설치합니다.
disable-model-invocation: true
---

# setup-claude

아래 두 스킬을 순서대로 실행합니다.

## Step 1: Statusline 설정

`setup-statusline` 스킬을 실행합니다.

## Step 2: LSP 설정

`setup-lsp` 스킬을 실행합니다.
